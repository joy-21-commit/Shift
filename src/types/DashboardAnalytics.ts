export interface DashboardAnalytics {
  averageScore: number;
  bestScore: number;
  worstScore: number;

  totalDrives: number;
  totalEvents: number;
  totalTime: number;

  mostCommonEvent: string;
}
